<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Action -->
    <record id="action_hospital_readonly_patient" model="ir.actions.act_window">
        <field name="name">View Patients</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="search_view_id" ref="om_hospital.view_hospital_patient_readonly_search" />
    </record>

   
    <!-- Search View -->
    <record id="view_hospital_patient_readonly_search" model="ir.ui.view">
        <field name="name">hospital.patient.readonly.search</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" filter_domain="['|',('name','ilike', self),('ref','ilike', self)]" />
                <field name="age" />
                <field name="gender" />
                <filter name="filter_male" string="Male" domain="[('gender', '=', 'male')]" />
                <filter name="filter_female" string="Female" domain="[('gender', '=', 'female')]" />
                <filter name="filter_others" string="Others" domain="[('gender', '=', 'others')]" />
                <separator />
                <filter name="filter_childs" string="Childs" domain="[('is_child', '=', True)]" />
                <group expand="0" string="Group By...">
                    <filter name="group_by_gender" string="Gender" context="{'group_by':'gender'}" />
                </group>
            </search>
        </field>
    </record>

   

   

    <!-- Tree View -->
    <record id="view_hospital_patient_readonly_tree" model="ir.ui.view">
        <field name="name">hospital.patient.readonly.tree</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <tree string="View Patients Tree">
                <field name="name" string="Name"/>
                <field name="is_child" string="Is Child?"/>
                <field name="date_of_birth" string="DOB"/>
                <field name="age" string="Age" readonly="1"/>
                <field name="doctor_id" string="Doctor"/>
                <field name="capitalized_name" string="Capitalized Name"/>
            </tree>
        </field>
    </record>


    <!-- Form View -->
    <record id="view_hospital_patient_readonly_form" model="ir.ui.view">
        <field name="name">hospital.patient.readonly.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form  string="View Patients Form">
                <sheet>
                    <div class="oe_title" style="display: flex; align-items: center; width: 100%;">
                        <div style="flex: 1; display: flex; align-items: center;">
                            <label for="name" class="oe_edit_only" />
                            <h1 style="margin: 0;">
                                <field name="name" />
                            </h1>
                        </div>
                        <div class="oe_right" style="flex: 1; display: flex; justify-content: flex-end;">
                            <field name="image" widget="image" class="oe_avatar" options="{'preview_image': 'image', 'size': [128, 128]}" />
                        </div>
                    </div>
                    <group>
                        <field name="is_child" />
                        <field name="date_of_birth" string="DOB" />
                        <field name="age" string="Age" readonly="1" />
                        <field name="doctor_id" />
                        <field name="capitalized_name" />
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" />
                    <field name="message_ids" options="{'post_refresh': 'recipients'}" />
                </div>
            </form>
        </field>
    </record>

    <!-- Kanban View -->
    <record id="view_hospital_patient_readonly_kanban" model="ir.ui.view">
        <field name="name">hospital.patient.readonly.kanban</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <kanban  string="view Patient Kanban">
                <field name="name" />
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card">
                            <strong>
                                <field name="name" options="{'truncate': True}" />
                            </strong>
                            <div><field name="is_child" widget="boolean" /></div>
                             <field name="date_of_birth" string="DOB" />
                            <div><field name="age" string="Age" readonly="1" /></div>
                            <div><field name="id" /></div>
                            <div><field name="capitalized_name" /></div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>



   

</odoo>
